// Type declaration for client.mjs (JS module — no TS source).
import type { ResolvedCredential } from '@platform/core'

export declare function makeGmailClient(credential: ResolvedCredential): Promise<unknown>
